# STL library (extra base add-ons)

Drop `.stl` files here (optionally in subfolders — folder name becomes category).

Regenerate the manifest after adding files:

```bash
npm run manifest:stl
```

Then commit `stls/` and `stls/library-manifest.json`.
